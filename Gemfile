source "https://rubygems.org/"

gem 'rake'
gem 'docopt'
gem 'mechanize'
gem 'geocoder'
gem 'geoip'
gem 'whois'
gem 'netaddr'
gem 'net-dns'
gem 'httparty'
gem 'parallel'

group :development do
  if RUBY_VERSION =~ /^1.9/
    gem "ruby-debug19", :require => "ruby-debug"
  else
    gem 'byebug'
  end
end

group :test do
end
